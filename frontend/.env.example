# 后端 API 根地址（不要末尾斜杠）。本机联调示例：
VITE_API_BASE=http://127.0.0.1:8000

# 扫码看广：流量主专属二维码 code（挂载到改写字数页 H5 观看）
VITE_ADHUB_QR_CODE=1zbovxyaowp
# H5 观看页根地址；不配则走 downAiGC 8000/static/adhub-watch（API 仍经 8000 代理到 8001）
# 微信手机扫码必须用局域网 IP 或公网 HTTPS，不能用 127.0.0.1
VITE_ADHUB_WATCH_BASE=http://10.20.92.59:8001

# 管理员后台邮箱白名单（逗号分隔，全小写）。需与后端 ADMIN_EMAILS 一致；不配则仅允许登录邮箱为 kiter 的本地种子账号
# VITE_ADMIN_EMAILS=admin@example.com

# Cloudflare Turnstile 站点密钥（可选；不配则前端可走本地数学题验证码）
# VITE_TURNSTILE_SITE_KEY=
